version: '3.8'

services:
  postgres_db:
    image: postgres:15-alpine
    container_name: study_golang_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
  redis:
    image: redis:7-alpine
    container_name: study_golang_redis
    restart: always
    ports:
      - "6379:6379"


volumes:
  pgdata:
